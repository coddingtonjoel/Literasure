import React from "react";

const Images = () => {
    return (
        <div className="images">
            IMAGES PLACEHOLDER [half / half] with diagonal line between two
            images
        </div>
    );
};

export default Images;
